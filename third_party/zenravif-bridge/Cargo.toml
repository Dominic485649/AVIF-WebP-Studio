[package]
name = "zenravif_bridge"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["staticlib"]

[profile.release]
panic = "abort"

[dependencies]
zenravif = { version = "0.1.3", default-features = false, features = ["threading", "imazen"] }
